[default]
# chunking
chunker = "semantic"
chunk_size = 450
chunk_overlap = 0

# dense retriever
dense_retriever_name = "jinaai/jina-clip-v1"

# causal model
causal_model_name = "meta-llama/Meta-Llama-3.1-8B-Instruct"

# vector store
vector_store = "chromadb"
retrieve_n = 5
hybrid = true

# lancedb
embbeding_dim = 512

# chromadb
chroma_db_path = "database"
chroma_collection_name = "my_store"
